version: '3.8'

services:
  db:
    image: postgres:18
    container_name: json-postgres
    restart: always
    environment:
      POSTGRES_DB: json_db
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"